A Java Spring Boot web application and API for managing a book database. Book data is obtained through Google Books API.

PostgreSQL was used as the database system and the website's appearance on the client-side was developed using CSS and HTML.
